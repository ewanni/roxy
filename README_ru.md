# ROXY - DPI Proxy с обходом блокировок

[![Rust](https://img.shields.io/badge/rust-1.75+-orange)](https://rust-lang.org)
[![License](https://img.shields.io/badge/license-MIT-blue)](Cargo.toml)

**ROXY** — высокопроизводительный прокси-сервер для обхода DPI (Deep Packet Inspection), написанный на Rust. Поддерживает SOCKS5, шифрование TLS 1.3, протокол QUIC (экспериментально) и продвинутый терминальный интерфейс для мониторинга в реальном времени.

[🇬🇧 English version]([`README.md`](README.md))

## 🌟 Возможности

- **🔐 Надежная безопасность**
  - TLS 1.3 с шифрованием ChaCha20-Poly1305
  - Аутентификация SCRAM-SHA-256
  - Поддержка сертификатов клиента
  - Ключи сессии с perfect forward secrecy

- **🚀 Обход DPI**
  - Обфускация и паддинг трафика
  - Имитация временных паттернов
  - Настройка TLS fingerprint
  - Полиморфизм протокола

- **🔌 Множество протоколов**
  - Нативный протокол ROXY
  - SOCKS5 клиент/сервер
  - Транспорт QUIC (экспериментально, через `quic-experimental`)
  - Прозрачное проксирование

- **📊 Продвинутый мониторинг**
  - Терминальный интерфейс (TUI) в реальном времени
  - Отслеживание сессий пользователей
  - Мониторинг пропускной способности
  - Метрики соединений
  - Удаленный мониторинг через HTTP API (с `tui-remote`)

- **⚡ Высокая производительность**
  - Асинхронный I/O на Tokio
  - Многопоточная обработка соединений
  - Zero-copy оптимизации
  - Эффективное использование памяти

- **🛡️ Защита**
  - Rate limiting и защита от DoS
  - Контроль доступа на уровне пользователя
  - Ограничение пропускной способности
  - Лимиты соединений

## 📋 Содержание

- [Быстрый старт](#-быстрый-старт)
- [Установка](#-установка)
- [Конфигурация](#-конфигурация)
- [Использование](#-использование)
- [Развертывание в Docker](#-развертывание-в-docker)
- [Архитектура](#-архитектура)
- [Разработка](#-разработка)
- [Безопасность](#-безопасность)
- [FAQ](#-faq)
- [Участие в проекте](#-участие-в-проекте)
- [Лицензия](#-лицензия)

## 🚀 Быстрый старт

### Требования

- Rust 1.75+ ([установить](https://rustup.rs/))
- Linux, macOS или Windows (рекомендуется WSL)

### Сборка из исходников

```bash
# Сборка с базовыми возможностями
cargo build --release

# Или сборка со всеми возможностями
cargo build --release --features quic-experimental,tui-remote

# Бинарный файл будет в target/release/roxy
```

### Генерация TLS сертификатов

```bash
# Создать директорию для сертификатов
mkdir -p certs

# Генерация самоподписанного сертификата (для тестов)
openssl req -x509 -newkey rsa:4096 -nodes \
  -keyout certs/server.key \
  -out certs/server.crt \
  -days 365 \
  -subj "/CN=localhost"

# Для продакшена используйте Let's Encrypt или свой CA
```

### Создание конфигурации и добавление первого пользователя

```bash
# Создать директорию конфигурации
mkdir -p config

# Добавить пользователя интерактивно
./target/release/roxy user --config config/config.yml add --name alice
# Введите пароль по запросу
```

### Запуск сервера

```bash
# Запуск с конфигом по умолчанию
./target/release/roxy server --config config/config.yml

# Или указать TLS сертификаты
./target/release/roxy server \
  --config config/config.yml \
  --tls-cert certs/server.crt \
  --tls-key certs/server.key \
  --port 8443
```

### Запуск TUI панели

```bash
# Мониторинг локального сервера
./target/release/roxy tui --config config/config.yml

# Или мониторинг удаленного сервера (требует tui-remote)
./target/release/roxy tui --remote http://server:9090
```

### Подключение клиента

```bash
# Подключиться к серверу
./target/release/roxy client \
  --server localhost:8443 \
  --user alice \
  --password <ваш-пароль>

# Или использовать переменную окружения для пароля
export ROXY_PASSWORD=<ваш-пароль>
./target/release/roxy client --server localhost:8443 --user alice
```

## 📦 Установка

### Из исходников

```bash
cargo install --path . --features quic-experimental,tui-remote
```

### Через Docker

```bash
# Собрать образ
docker build -t roxy:latest .

# Запустить сервер
docker run -d \
  -p 8443:8443 \
  -p 4433:4433/udp \
  -v $(pwd)/config:/app/config \
  -v $(pwd)/certs:/app/certs \
  --name roxy-server \
  roxy:latest
```

### Через Docker Compose

```bash
# Запустить все сервисы
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановить сервисы
docker-compose down
```

См. [Развертывание в Docker](#-развертывание-в-docker) для подробностей.

## ⚙️ Конфигурация

ROXY использует YAML файлы конфигурации для сервера и управления пользователями.

### Конфигурация сервера ([`config/config.yml`](config/config.yml))

```yaml
# ROXY Configuration
users: {}
session_lifetime: 3600
alpn_protocols: ["h2", "http/1.1"]
log_level: "INFO"
log_theme_path: "config/logging_theme.yml"
log_to_file: false
log_file_path: null

server:
  bind_address: "0.0.0.0"
  port: 8443
  max_concurrent_connections: 1000
  buffer_size: 8192

tls:
  enabled: true
  cert_path: "certs/server.crt"
  key_path: "certs/server.key"
  versions: ["1.3", "1.2"]

timeouts:
  connect_timeout: 10
  read_timeout: 30
  write_timeout: 30
  idle_timeout: 300

quic:
  enabled: false
  bind_address: "0.0.0.0"
  port: 4433
  idle_timeout_ms: 30000

socks5:
  enabled: true
  bind_addr: "0.0.0.0:1080"
  server_addr: "roxy-server:1081"
  username: ""
  password: ""
  server_enabled: true
  server_bind_addr: "0.0.0.0:1081"

allow_plain_http: true
default_bandwidth_limit_mbps: null
```

### Конфигурация пользователей

Пользователи хранятся в `config/config.yml` с SCRAM credentials:

```yaml
users:
  alice:
    scram:
      salt: "a1b2c3d4..."
      iterations: 100000
      stored_key: "..."
      server_key: "..."
    permissions:
      allowed_routes:
        - "tcp:*:443"
        - "tcp:example.com:80"
      bandwidth_limit_mbps: 10
    active: true
    expires_at: null  # ISO 8601 datetime или null
```

**Важно:** Используйте `roxy user add` для генерации корректных credentials. Никогда не редактируйте SCRAM поля вручную.

## 🎮 Использование

### Режим сервера

```bash
# Запуск с кастомной конфигурацией
roxy server --config config/config.yml --port 8443

# С кастомными TLS сертификатами
roxy server \
  --config config/config.yml \
  --tls-cert /path/to/cert.pem \
  --tls-key /path/to/key.pem
```

### Режим клиента

```bash
# Базовое подключение
roxy client --server example.com:8443 --user alice --password secret

# Запрос конкретных маршрутов
roxy client \
  --server example.com:8443 \
  --user alice \
  --routes "tcp:google.com:443,tcp:github.com:443"

# Пропуск проверки сертификата (ОПАСНО - только для тестов)
roxy client --server localhost:8443 --user alice --skip-cert-verification
```

### Режим TUI (терминальный интерфейс)

```bash
# Мониторинг локального сервера
roxy tui --config config/config.yml

# Мониторинг удаленного сервера (требует tui-remote)
roxy tui --remote http://server.example.com:9090
```

**Управление TUI:**
- `Tab` / `Shift+Tab` - Переключение между экранами
- `↑/↓` или `j/k` - Навигация по спискам
- `Enter` - Выбор элемента
- `q` или `Esc` - Выход
- `r` - Обновить метрики

### Управление пользователями

```bash
# Добавить нового пользователя
roxy user --config config/config.yml add --name bob

# Пользователи сохраняются в config/config.yml с SCRAM credentials
# Редактируйте права доступа, даты истечения вручную в YAML (но не SCRAM поля!)
```

## 🐳 Развертывание в Docker

### Использование Dockerfile

Проект включает многоэтапный Dockerfile, оптимизированный для минимального размера образа и безопасности.

```bash
# Собрать образ
docker build -t roxy:latest .

# Запустить сервер
docker run -d \
  -p 8443:8443 \
  -p 4433:4433/udp \
  -v $(pwd)/config:/app/config:ro \
  -v $(pwd)/certs:/app/certs:ro \
  --restart unless-stopped \
  --name roxy-server \
  roxy:latest
```

### Использование Docker Compose

[`docker-compose.yml`]([`docker-compose.yml`](docker-compose.yml)) предоставляет полную конфигурацию развертывания:

```yaml
version: '3.8'

services:
  roxy-server:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        FEATURES: "tui-remote,quic-experimental"
    ports:
      - "8443:8443"      # ROXY сервер
      - "4433:4433/udp"  # QUIC (опционально)
      - "9090:9090"      # API метрик (tui-remote)
    volumes:
      - ./config:/app/config:ro
      - ./certs:/app/certs:ro
    environment:
      - RUST_LOG=info
    restart: unless-stopped
```

**Развертывание:**

```bash
docker-compose up -d
docker-compose logs -f roxy-server
docker-compose exec roxy-server roxy user --config /app/config.yml add --name alice
```

## 🏗️ Архитектура

ROXY реализует кастомный DPI-устойчивый протокол с множеством уровней безопасности и обфускации:

```
┌─────────────────────────────────────┐
│   Данные приложения (HTTP и т.д.)  │ ← Пользовательский трафик
├─────────────────────────────────────┤
│      Протокол ROXY (приложение)     │ ← Аутентиф., фреймы, обфускация
├─────────────────────────────────────┤
│         Шифрование TLS 1.3          │ ← ChaCha20-Poly1305
├─────────────────────────────────────┤
│         TCP (Порт 8443)             │ ← Транспорт
└─────────────────────────────────────┘
```

### Ключевые компоненты

- [`src/server.rs`](src/server.rs) - Асинхронный TLS сервер с обработкой соединений
- [`src/client.rs`](src/client.rs) - Реализация клиента с SCRAM аутентификацией
- [`src/protocol.rs`](src/protocol.rs) - Определения фреймов протокола ROXY
- [`src/auth.rs`](src/auth.rs) - Аутентификация SCRAM-SHA-256
- [`src/crypto.rs`](src/crypto.rs) - Криптографические примитивы
- [`src/obfuscation/`](src/obfuscation/) - Формирование трафика и обфускация
- [`src/transport/`](src/transport/) - Реализации SOCKS5 и QUIC
- [`src/tui/`](src/tui/) - Терминальный интерфейс

### Поток протокола

1. **TLS Handshake** - Стандартный TLS 1.3 (имитация HTTPS)
2. **ROXY Handshake** - Аутентификация клиента через SCRAM-SHA-256
3. **Установка сессии** - Вывод ключей сессии, согласование обфускации
4. **Туннель данных** - Зашифрованные фреймы данных с обфускацией
5. **Завершение** - Graceful закрытие соединения

Подробная спецификация протокола в [`docs/protocol-design.md`](docs/protocol-design.md).

## 🔧 Разработка

### Сборка

```bash
# Debug сборка
cargo build

# Release сборка (оптимизированная)
cargo build --release

# Со всеми возможностями
cargo build --release --all-features

# Проверка кода без сборки
cargo check
```

### Тестирование

```bash
# Запустить все тесты
cargo test

# Запустить тесты с выводом
cargo test -- --nocapture

# Запустить конкретный тест
cargo test test_scram_authentication

# Запустить с фичами
cargo test --features quic-experimental
```

### Линтинг

```bash
# Проверить стиль кода
cargo clippy -- -D warnings

# Форматировать код
cargo fmt

# Проверить форматирование
cargo fmt -- --check
```

### Документация

```bash
# Сгенерировать и открыть документацию
cargo doc --open --no-deps

# Включить приватные элементы
cargo doc --open --document-private-items
```

### Бенчмарки

```bash
# Запустить бенчмарки (требуется nightly)
cargo +nightly bench
```

## 🔒 Безопасность

### Функции безопасности

- **Шифрование:** TLS 1.3 + ChaCha20-Poly1305 на уровне приложения
- **Аутентификация:** SCRAM-SHA-256 с солированным хэшированием паролей
- **Forward Secrecy:** Эфемерные ключи сессии через HKDF
- **Rate Limiting:** Защита от DoS с алгоритмом token bucket
- **Контроль доступа:** Права доступа к маршрутам на уровне пользователя
- **Аудит:** Подробное логирование событий аутентификации

### Лучшие практики безопасности

1. **Никогда не используйте в продакшене без легитимных TLS сертификатов**
2. **Регулярно меняйте пароли пользователей**
3. **Включайте rate limiting и лимиты соединений**
4. **Мониторьте логи на предмет подозрительной активности**
5. **Обновляйте зависимости** (`cargo update`)
6. **Используйте сильные, уникальные пароли для каждого пользователя**
7. **Ограничивайте `allowed_routes` до минимально необходимого**

### Сообщения об уязвимостях

Сообщайте о проблемах безопасности приватно автору проекта.

Пожалуйста, **не создавайте** публичные issue для уязвимостей.

## ❓ FAQ

**В: Что такое DPI и зачем его обходить?**
О: Deep Packet Inspection (DPI) анализирует сетевой трафик для обнаружения и блокировки определенных протоколов. ROXY маскирует трафик под обычный HTTPS для обхода обнаружения.

**В: ROXY это VPN?**
О: Нет, ROXY это прокси-сервер. Требуется ROXY клиент или совместимый SOCKS5 клиент для подключения.

**В: Можно ли использовать ROXY с существующими приложениями?**
О: Да, через режим SOCKS5 прокси. Настройте приложение на использование `localhost:1080` как SOCKS5 прокси.

**В: В чем разница между протоколом ROXY и SOCKS5?**
О: Протокол ROXY включает обход DPI, аутентификацию и шифрование. SOCKS5 — стандартный протокол прокси с опциональной интеграцией.

**В: Поддержка QUIC стабильна?**
О: QUIC экспериментальный (`quic-experimental`). Используйте TCP+TLS для продакшена.

**В: Как обновить права пользователя?**
О: Отредактируйте [`config/config.yml`](config/config.yml) и перезапустите сервер. Не изменяйте SCRAM credentials поля.

**В: Можно ли запустить несколько серверов?**
О: Да, используйте разные порты или IP адреса в конфигурации.

**В: Какие уровни логирования доступны?**
О: `TRACE`, `DEBUG`, `INFO` (по умолчанию), `WARN`, `ERROR`. Устанавливается через `log_level` в [`config/config.yml`](config/config.yml).

## 🤝 Участие в проекте

Вклады приветствуются! Пожалуйста, следуйте этим указаниям:

1. Сделайте fork репозитория
2. Создайте ветку функциональности (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

### Руководство по разработке

- Следуйте конвенциям кода Rust
- Добавляйте тесты для новых функций
- Обновляйте документацию
- Запускайте `cargo clippy` и `cargo fmt` перед коммитом
- Пишите понятные commit сообщения

## 📄 Лицензия

Этот проект лицензирован под MIT License - см. [Cargo.toml](Cargo.toml) для деталей.

## 🙏 Благодарности

- [Tokio](https://tokio.rs/) - Асинхронная среда выполнения
- [rustls](https://github.com/rustls/rustls) - Реализация TLS
- [ratatui](https://github.com/ratatui-org/ratatui) - Фреймворк терминального интерфейса
- [Tor Project](https://www.torproject.org/) - Вдохновение для техник обфускации

---
**⚠️ Отказ от ответственности:** Это программное обеспечение предоставляется в образовательных и исследовательских целях. Пользователи несут ответственность за соблюдение местных законов и правил. Авторы не несут ответственности за неправомерное использование.
